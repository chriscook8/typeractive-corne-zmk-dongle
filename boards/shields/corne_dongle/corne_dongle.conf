# Corne Dongle Configuration

# Enable USB output
CONFIG_ZMK_USB=y

# Enable BLE for connecting to peripherals
CONFIG_ZMK_BLE=y

# Central role settings
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

# Increase BLE TX power for better range (optional)
# CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
